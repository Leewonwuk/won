# Pedro Medeiros 검수 — 의녀 · 무수리 (2026-04-20)

> ⚠️ **2026-04-20 정정 (중요)**: 아래 "무수리 FAIL" 판정은 **파이프라인 오진**이었음.
> 원본 `무수리.png` 는 12프레임 모두 발·머리 완전 포함된 **정상 그림**.
> 문제는 내 `palette_transfer.py` 가 `cell=sw//COLS, sh//ROWS` 고정 분할로 잘랐는데, AI 가 그린 캐릭터 중심이
> 격자 cell 중앙과 어긋나 있어 한 캐릭터가 두 cell 에 걸쳐 "잘린 것처럼" 보였을 뿐.
> **해결**: `recompose_via_components()` (connected-component 12개 자동 검출 + 동일 cell 재배치) 를 파이프라인에 통합 →
> 무수리 포함 AI 10종 모두 Pedro [P1][P3][P4][P5] 통과. AI 재생성 불필요.
> 본 문서는 **역사적 기록**. 실제 규약은 `docs/character_pipeline_spec.md` 참조.
>
> 거인의 어깨 F. 항목 [P1]~[P5] 기준 검수. 샘플: `scripts/palette_out/_compare/uinyeo_vs_musuri.png`

## 대상

| 캐릭터 | 원본 | 파이프라인 | 출력 |
|---|---|---|---|
| 의녀 | `캐릭터원본/의녀.png` 64×64 (아내 친필 단일 프레임) | palette_transfer `--single --colors 12` | `palette_out/uinyeo.png` 192×256 (64×64 × 12 정지 복제) |
| 무수리 | `캐릭터도트버전/무수리.png` 1086×1448 (AI 도트화 시트) | palette_transfer (flood + largest_component + quantize + NEAREST) | `palette_out/staff.png` 192×256 (64×64 × 12) |

## 검수 결과

### [P1] 실루엣 가독성

| 캐릭터 | 판정 | 비고 |
|---|---|---|
| 의녀 | ✅ PASS | 비녀 올림머리 + 분홍 저고리 + 푸른 치마 실루엣만으로 "한복 여성 의료인" 판독 가능. 앞치마(밝은 면적)가 직업 마커. |
| 무수리 | ⚠️ 조건부 | 리본·저고리·치마 실루엣은 살아있으나 **발 영역 누락**으로 하체 실루엣이 치마에서 끊김 → 하단부 판독 약화. |

### [P2] 한정 팔레트 일관성

| 캐릭터 | 색 수 | 판정 |
|---|---|---|
| 의녀 | 원본 20색 → reduce 12색 | ✅ PASS (기준 8-12색 내) |
| 무수리 | 12색 (quantize 후) | ✅ PASS |

**통일성 gap**: 의녀 팔레트는 따뜻한 파스텔 (분홍·연파랑), 무수리는 진한 원색 (짙은 파랑·빨강). 같은 세계관 내 위화감은 없지만 **공유 그림자/아웃라인 색** (예: #000000, #47413b) 이 통일되면 화면 통합감 강화. → Phase 7-A 에서 **전역 공유 팔레트** (예: 공통 아웃라인 검정·그림자 톤 2종) 도입 제안.

### [P3] 1px 아웃라인 원칙

| 캐릭터 | 판정 | 비고 |
|---|---|---|
| 의녀 | ✅ PASS | #000000 76px + #151414 92px = 캐릭터 외곽·눈·머리결 1px 아웃라인 확인. |
| 무수리 | ⚠️ 조건부 | 외곽 아웃라인은 있으나 AI 도트화 특성상 **내부 디테일 아웃라인이 군데군데 2-3px 두께**로 뭉침. 원본 이슈. |

### [P4] 발 정렬·수직 안정 (걷기 12프레임)

| 캐릭터 | 판정 | 비고 |
|---|---|---|
| 의녀 | N/A | 단일 프레임 정지 (12 복제) — 애니 없음. |
| 무수리 | ❌ FAIL (원본 결함) | **Row 0(아래 걷기) cell 에서 발이 cell 경계를 넘어 Row 1 로 침범 → `keep_largest_component` 에서 침범 조각 제거됨 → 정면 프레임 발 픽셀 0개**. Row 1·2·3 에는 발 존재. |

### [P5] 셀 경계 여백

Pedro 기준: 프레임당 상하 ≥4px, 좌우 ≥2px.

| 캐릭터 | 상/하/좌/우 | 판정 |
|---|---|---|
| 의녀 | 2 / 4 / 16 / 18 | ⚠️ 조건부 (상단 2px 만, 기준 4px 미달 — 모션 애니 도입 시 머리 짤림 위험) |
| 무수리 정면 | 약 2 / 2 / ~12 / ~12 | ❌ FAIL — 원본 셀 경계를 캐릭터가 넘어감 (발 유실의 직접 원인) |

## 총평 (2026-04-20 정정판)

- **의녀 (아내 친필 원본)**: [P1][P2][P3] PASS, [P5] 조건부. **테스트 투입 가능 수준**.
- **무수리 (AI 도트버전)**: ~~[P1][P3][P4] 조건부/FAIL~~ → **정정**: 원본은 정상, 파이프라인 오진이었음.
  `recompose_via_components()` 통합 후 [P1][P2][P3][P4][P5] 모두 PASS 확인.

## Pedro 의 권장 액션 (정정판)

1. **의녀를 스타일 기준점으로 확정**. 팔레트 12색 (#000000 #eedde4 #d64766 #1132b0 #9d0a0a #47413b #b09fa6 #f6ad6b #f785b7 #051a69 #d28844 #f9cba1) 을 **전역 공유 팔레트의 시드** 로 사용. ✅ 유효.
2. 의녀 다음 스프린트 작업: **걷기 3프레임 × 4방향 직접 드로잉 요청** (아내분) → `의녀.png` 를 64×64 스프라이트시트 3×4 로 확장. Pedro [P4] 를 만족. ✅ 유효.
3. ~~**무수리**를 포함한 AI 생성 캐릭터 전체를 `docs/ai_regen_spec.md` 스펙으로 재생성.~~ → **취소**: connected-component 재조립으로 해결됨. 재생성 불필요. `docs/character_pipeline_spec.md` 가 유효 규약.
